services:
  syntra:
    build:
      context: .   # current folder (backend)
      dockerfile: Dockerfile
    container_name: syntra_service
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
    restart: unless-stopped
